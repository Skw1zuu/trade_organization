#pragma once
#include <string>

using namespace std;

class input
{
public:
    static void input_number(float& out, const string question);
    static void input_number(unsigned int& out, const string question);

};
